# Tasks Document: Market Data Module

## References

- **Issue:** STAK-504
- **Spec Path:** `.spec-workflow/specs/STAK-504-market-data-module/`

{/* VERSION CHECKOUT GATE — MANDATORY
Before implementing ANY task below, you MUST:
1. Run /release patch (or /start-patch) to claim a version and create a worktree
2. Record the assigned version (e.g., 3.34.01) in the first implementation log
3. ALL file edits happen inside the worktree — never in the main repo working directory
4. Verify: git branch --show-current returns patch/VERSION, not dev or main
5. If multiple tasks are parallelized across agents, each agent gets its own /release patch
Skipping this gate is a workflow violation. See CLAUDE.md Version Checkout Gate section.

SPEC COMPLETION GATE — BLOCKING (Phase 5):
After ALL tasks are [x] and implementation logs are recorded:
1. Run /vault-update to update all DocVault pages whose frontmatter sourceFiles match this spec's changed files
2. Close all linked vault issues (set status: done)
3. Verify /bb-test passes or file follow-up issues for any new failures
4. The spec is NOT complete until all three are verified.
*/}

---

## File Touch Map

| File | Action | Scope |
|------|--------|-------|
| `js/constants.js` | MODIFY | Add `marketData` to `LAYOUT_SECTION_DEFAULTS`, add `MARKET_DATA_COMPONENT_DEFAULTS`, add 7 new localStorage keys to `ALLOWED_STORAGE_KEYS`, add window exports |
| `js/market-data.js` | CREATE | Orchestrator + CSS-only components (retail ranges, goldback calc, best price ticker) |
| `js/market-charts.js` | CREATE | Lightweight Charts + ApexCharts wrappers (enhanced spot cards, premium heat map) |
| `js/settings.js` | MODIFY | Add `marketData` to `sectionMap` in `applyLayoutOrder()`, add market data component config table renderer |
| `js/init.js` | MODIFY | Call `initMarketData()` after spot data loads |
| `index.html` | MODIFY | Add 2 CDN `<script>` tags, add `<section id="marketDataSectionEl">`, add 2 `<script defer>` for new JS files, add Settings fieldset for market data component picker |
| `css/styles.css` | MODIFY | Market data grid styles, component styles, theme-specific color overrides for all 3 themes |
| `sw.js` | MODIFY | Add 2 new JS files to `CORE_ASSETS` |

---

## StakTrakr Critical Patterns (applies to all tasks)

- **DOM access**: `safeGetElement('id')` — never `document.getElementById()`
- **Storage reads/writes**: `saveData(key, val)` / `loadData(key)` from `js/utils.js`
- **New storage keys**: must be added to `ALLOWED_STORAGE_KEYS` in `js/constants.js`
- **innerHTML**: always wrap user content in `sanitizeHtml()`
- **New JS files**: add to BOTH `index.html` (correct load-order position) AND `sw.js` CORE_ASSETS
- **Duplicate check**: before editing `events.js` or `api.js`, grep for the function name in both files
- **Variable declarations**: always use `const`/`let` — `var` is banned

---

## Phase 0 — Visual Prototype Gate

- [x] 0.1. Create interactive prototype playground
  - File: `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html`
  - Build a self-contained HTML file demonstrating all 5 market data components with live v2 API data. Use the existing `market-data-playground.html` in repo root as a starting reference but rebuild with: (a) TradingView Lightweight Charts for spot sparklines instead of Chart.js, (b) ApexCharts for the premium heat map, (c) all 3 theme support (dark/light/sepia toggle), (d) mobile-first responsive grid, (e) saturated high-contrast colors in dark mode, (f) fix scaling and contrast issues noted in the original prototype.
  - Purpose: Visual approval gate — the approved prototype becomes the source of truth for all UI implementation tasks.
  - _Leverage: `market-data-playground.html` (repo root) for component structure and v2 API integration; `css/styles.css:1-230` for theme CSS custom property values; `index.html:465-540` for existing spot card DOM structure_
  - _Requirements: REQ-3, REQ-4, REQ-5, REQ-6, REQ-7, REQ-8, REQ-9_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend designer/developer | Task: Create an interactive prototype playground at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html`. This is a self-contained HTML file (no external dependencies except CDN chart libraries) that demonstrates all 5 market data components with live v2 API data from `api.staktrakr.com`. Read the existing `market-data-playground.html` in repo root for reference — it has working v2 API integration and 6 components (drop the vendor comparison bar chart, keep the other 5). Key changes from the reference: (1) Replace Chart.js with TradingView Lightweight Charts (CDN: `https://cdn.jsdelivr.net/npm/lightweight-charts@4/dist/lightweight-charts.standalone.production.js`) for the Enhanced Spot Cards sparkline backgrounds. Use `createChart()` with `autoSize: true`, add an `AreaSeries` with metal-colored fill. (2) Replace the CSS heat map grid with ApexCharts heatmap (CDN: `https://cdn.jsdelivr.net/npm/apexcharts@4/dist/apexcharts.min.js`). Configure `plotOptions.heatmap.colorScale.ranges` with green→yellow→red for premium %. Use `chart.type: 'heatmap'`. (3) Add a theme toggle (dark/light/sepia) using the exact CSS custom property values from `css/styles.css` lines 1-230. Dark mode MUST use saturated, high-contrast colors — not faded or muted. (4) Mobile-first grid: single column default, 2-column at 768px+. Spot cards: 2-col mobile, 4-col at 960px+. (5) No dead space when a component is hidden — components reflow to fill width. (6) Fix the scaling and contrast issues from the original prototype. Include a component toggle panel so the user can show/hide each component and verify the reflow behavior. | Restrictions: This is a prototype file only — do NOT modify any production files. The playground must be fully self-contained (inline CSS and JS, CDN script tags only). Do not use Chart.js. Do not import node modules. | Success: Playground opens in a browser, fetches live v2 data, renders all 5 components, theme toggle works across all 3 themes with correct colors, responsive grid works at mobile/tablet/desktop widths, component toggles cause clean reflow with no gaps. PREREQUISITE: This is a prototype task — no worktree needed. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 0.2. User visual approval
  - File: (no file changes — review only)
  - Present the playground prototype to the user. Collect explicit visual approval. If revisions needed, iterate on 0.1.
  - Purpose: Ensure the visual design meets user expectations before any production code is written.
  - _Requirements: REQ-8, REQ-9_
  - _Prompt: Present the playground prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` to the user for visual review. Open the file and walk through each component, theme, and responsive breakpoint. Ask for explicit approval or revision requests. If revisions needed, update the playground and re-present. Do NOT proceed to implementation tasks until the user explicitly approves the visual design._

---

## Phase 1 — Framework (sequential — sets up structure for all components)

- [x] 1. Constants and config infrastructure
  - File: `js/constants.js`
  - Add `{ id: 'marketData', label: 'Market Data', enabled: true }` to `LAYOUT_SECTION_DEFAULTS` array (after `table` entry, ~line 1183). Add `MARKET_DATA_COMPONENT_DEFAULTS` array with 5 entries (enhancedSpot, retailRanges, premiumHeatMap, goldbackCalc, bestPriceTicker). Add `getMarketDataComponentConfig()` and `saveMarketDataComponentConfig()` functions using `_loadSectionConfig`/`_saveSectionConfig`. Add 7 new keys to `ALLOWED_STORAGE_KEYS`: `marketDataComponentConfig`, `v2SpotHistory_xag`, `v2SpotHistory_xau`, `v2SpotHistory_xpt`, `v2SpotHistory_xpd`, `v2SpotHistoryTs`, `v2GoldbackData`. Add window exports for all new constants and functions.
  - Purpose: Establish the config infrastructure that all subsequent tasks depend on.
  - _Leverage: `_loadSectionConfig()` at `js/constants.js:1212`; `LAYOUT_SECTION_DEFAULTS` at `js/constants.js:1178`; `ALLOWED_STORAGE_KEYS` at `js/constants.js:889`; window exports block at `js/constants.js:1823`_
  - _Requirements: REQ-1, REQ-2, REQ-10_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend infrastructure developer | Task: Edit `js/constants.js` to add the market data module config infrastructure. (1) Add `{ id: 'marketData', label: 'Market Data', enabled: true }` to the `LAYOUT_SECTION_DEFAULTS` array after the `table` entry (~line 1183). (2) Add a new section after the layout config block (~line 1250) with `MARKET_DATA_COMPONENT_DEFAULTS` containing 5 entries: `{ id: 'enhancedSpot', label: 'Enhanced Spot Cards', enabled: true }`, `{ id: 'retailRanges', label: 'Retail Price Ranges', enabled: true }`, `{ id: 'premiumHeatMap', label: 'Premium Heat Map', enabled: true }`, `{ id: 'goldbackCalc', label: 'Goldback Calculator', enabled: true }`, `{ id: 'bestPriceTicker', label: 'Best Price Ticker', enabled: true }`. (3) Add `getMarketDataComponentConfig()` using `_loadSectionConfig('marketDataComponentConfig', MARKET_DATA_COMPONENT_DEFAULTS)` and `saveMarketDataComponentConfig(config)` using `_saveSectionConfig('marketDataComponentConfig', config)`. (4) Add 7 keys to `ALLOWED_STORAGE_KEYS`: `marketDataComponentConfig`, `v2SpotHistory_xag`, `v2SpotHistory_xau`, `v2SpotHistory_xpt`, `v2SpotHistory_xpd`, `v2SpotHistoryTs`, `v2GoldbackData`. (5) Add window exports for `MARKET_DATA_COMPONENT_DEFAULTS`, `getMarketDataComponentConfig`, `saveMarketDataComponentConfig` in the exports block (~line 1870+). | Restrictions: Do NOT modify `LAYOUT_SECTION_DEFAULTS` entries for existing sections. Do NOT rename or move existing functions. Use `const` for all declarations. | Success: `LAYOUT_SECTION_DEFAULTS` has 5 entries (spotPrices, totals, search, table, marketData). `MARKET_DATA_COMPONENT_DEFAULTS` has 5 entries. All 7 new keys in `ALLOWED_STORAGE_KEYS`. All new symbols exported on `window`. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 2. HTML section shell and CDN script tags
  - File: `index.html`, `sw.js`
  - Add `<section id="marketDataSectionEl" class="market-data-section">` to `index.html` between `tableSectionEl` (line ~958) and the footer. Add a container div `<div id="marketDataContainer" class="market-data-grid"></div>` inside. Add 2 CDN `<script>` tags in `<head>`: Lightweight Charts and ApexCharts. Add 2 `<script defer>` tags for `js/market-charts.js` and `js/market-data.js` in the correct load order (after `chart-utils.js`, before `events.js`). Add both new JS files to `CORE_ASSETS` in `sw.js`.
  - Purpose: Provide the DOM skeleton and script registration for the market data module.
  - _Leverage: `index.html:465-540` for existing section structure; `index.html` script block for load order; `sw.js` CORE_ASSETS array_
  - _Requirements: REQ-1_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Edit `index.html` and `sw.js` to add the market data section shell and script registrations. (1) In `index.html`, add a new section after `tableSectionEl` (~line 958) and before the footer: `<section class="market-data-section" id="marketDataSectionEl"><div class="section-title">Market Data</div><div id="marketDataContainer" class="market-data-grid"></div></section>`. (2) In the `<head>`, add two CDN scripts BEFORE the closing `</head>`: `<script defer src="https://cdn.jsdelivr.net/npm/lightweight-charts@4/dist/lightweight-charts.standalone.production.js"></script>` and `<script defer src="https://cdn.jsdelivr.net/npm/apexcharts@4/dist/apexcharts.min.js"></script>`. (3) Add two `<script defer>` tags in the body script block after `chart-utils.js` and before `events.js`: `<script defer src="js/market-charts.js"></script>` then `<script defer src="js/market-data.js"></script>` (charts loads first since market-data depends on it). (4) In `sw.js`, add `'js/market-charts.js'` and `'js/market-data.js'` to the `CORE_ASSETS` array. | Restrictions: Do NOT move or modify existing sections. Do NOT change existing script order. Put CDN scripts in `<head>` with `defer`. | Success: New section visible in HTML source. 2 CDN tags in head. 2 new script tags in correct load order. Both files listed in sw.js CORE_ASSETS. Page loads without errors. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 3. Settings UI and layout integration
  - File: `js/settings.js`, `index.html`
  - Add `marketData: elements.marketDataSectionEl` to `sectionMap` in `applyLayoutOrder()` (~line 1478). Add a `renderMarketDataComponentConfigTable()` function using `_renderSectionConfigTable()` with `getMarketDataComponentConfig`/`saveMarketDataComponentConfig` and `onApply: refreshMarketData`. Add call to `renderMarketDataComponentConfigTable()` inside `syncLayoutVisibilityUI()`. Add the Settings HTML fieldset for "Market Data Components" in Settings > Appearance > Layout section of `index.html`.
  - Purpose: Connect the market data section to the layout toggle/reorder system and provide the sub-component config UI.
  - _Leverage: `applyLayoutOrder()` at `js/settings.js:1476`; `syncLayoutVisibilityUI()` at `js/settings.js:1263`; `_renderSectionConfigTable()` at `js/settings.js:1282`; `renderLayoutSectionConfigTable()` at `js/settings.js:1372` as pattern_
  - _Requirements: REQ-1, REQ-2_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Wire the market data section into settings.js and add the component picker UI. (1) In `applyLayoutOrder()` (~line 1478), add `marketData: elements.marketDataSectionEl` to the `sectionMap` object (use `safeGetElement('marketDataSectionEl')` — note: `elements` may not have this yet, so use `safeGetElement` directly or add it to the elements init). (2) Add `renderMarketDataComponentConfigTable()` function following the pattern of `renderLayoutSectionConfigTable()` (~line 1372): call `_renderSectionConfigTable({ containerId: 'marketDataComponentConfigContainer', getConfig: getMarketDataComponentConfig, saveConfig: saveMarketDataComponentConfig, onApply: typeof refreshMarketData === 'function' ? refreshMarketData : null, onRender: () => renderMarketDataComponentConfigTable() })`. Export on window. (3) Add `renderMarketDataComponentConfigTable()` call inside `syncLayoutVisibilityUI()` (~line 1263). (4) In `index.html`, within the Settings > Appearance > Layout section (near the existing "Visible sections" fieldset), add a new fieldset: `<div class="settings-fieldset"><div class="settings-fieldset-title">Market Data Components</div><div class="settings-group"><div class="settings-group-label">Visible components</div><p class="settings-subtext">Toggle visibility and reorder market data components.</p><div class="chip-grouping-table-container" id="marketDataComponentConfigContainer"><div class="chip-grouping-empty">Loading...</div></div></div></div>`. | Restrictions: Do NOT modify `applyLayoutOrder()` logic — only add to `sectionMap`. Do NOT rename existing functions. | Success: "Market Data" appears in Settings > Layout section toggles. Component picker shows 5 items with checkboxes and arrows. Toggling/reordering components persists to localStorage and triggers re-render. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

---

## Phase 2 — CSS Foundation (parallel-safe with Phase 1 tasks, touches only css/styles.css)

- [ ] 4. Market data CSS styles and theme support
  - File: `css/styles.css`
  - Add market data section styles: `.market-data-section`, `.market-data-grid` (mobile-first grid), `.market-data-component` (wrapper for each sub-component), `.md-spot-cards-grid` (2-col mobile, 4-col 960px+), `.md-spot-card` (matches existing `.spot-card` aesthetic), `.md-range-bar-*` (price range bars), `.md-heat-map-container` (scrollable on mobile), `.md-goldback-*` (denomination cards), `.md-ticker` (scrolling strip). Add all 3 theme variants. Dark mode: saturated high-contrast colors. Light/sepia: appropriate adjustments. All colors via CSS custom properties.
  - Purpose: Establish the complete CSS foundation so component rendering tasks can focus on JS logic.
  - _Leverage: `css/styles.css:1-230` for existing theme variables; `market-data-playground.html` for component CSS (refined); existing `.spot-card` styles for card aesthetic_
  - _Requirements: REQ-8, REQ-9_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: CSS/frontend designer | Task: Add all market data module CSS to `css/styles.css`. Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design — the prototype IS the source of truth. Source your class names, spacing, and layout from the prototype. Add styles at the end of the file, before any media query overrides. Key requirements: (1) `.market-data-section` — section container matching existing section styling. (2) `.market-data-grid` — CSS grid, mobile-first: `grid-template-columns: 1fr` default, `repeat(2, 1fr)` at 768px+. Gap matches existing spacing variables. (3) `.md-spot-cards-grid` — 2-col mobile, 4-col at 960px+ (matches existing `.spot-cards-grid`). (4) `.md-spot-card` — card with relative positioning for chart background, matching `.spot-card` aesthetic. (5) `.md-range-bar`, `.md-range-bar-fill`, `.md-range-bar-median`, `.md-range-bar-values` — horizontal range bars. (6) `.md-heat-map-container` — `overflow-x: auto` on mobile with `-webkit-overflow-scrolling: touch`. (7) `.md-goldback-grid` — denomination card grid. (8) `.md-ticker` — horizontal scrolling strip with CSS animation. (9) `.md-component-empty` — neutral empty state. (10) Theme overrides: in `[data-theme="dark"]` add market data color overrides using saturated, vivid colors — the heat map green-yellow-red scale must POP, not look faded. In `[data-theme="light"]` and `[data-theme="sepia"]` add appropriate contrast adjustments. Use ONLY existing CSS custom properties (`--bg-card`, `--border`, `--text-primary`, `--text-secondary`, `--text-muted`, `--success`, `--danger`, `--silver`, `--gold`, `--platinum`, `--palladium`) plus new market-data-specific properties if needed (`--md-heat-low`, `--md-heat-mid`, `--md-heat-high`). | Restrictions: Do NOT modify existing styles. Add new styles only. Use `var()` for ALL colors — no hardcoded hex values in component styles. All new classes prefixed with `md-` or `market-data-` to avoid conflicts. | Success: All market data components are fully styled in all 3 themes. Mobile-first grid works. No horizontal overflow on mobile (except heat map which scrolls). Dark mode colors are vivid and high-contrast. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

---

## Phase 3 — Components (tasks 5-9 are independent — can run in parallel after Phase 1+2)

- [ ] 5. Market data orchestrator and data fetching
  - File: `js/market-data.js` (CREATE)
  - Create the orchestrator module. `initMarketData()` fetches v2 spot history (4 metals, 7d), reads cached retail/goldback data from localStorage, then calls `renderMarketDataSection()`. `renderMarketDataSection()` reads the component config, clears the container, and renders each enabled component in order by delegating to the appropriate render function. `refreshMarketData()` re-renders from cached data (no fetch). Export `initMarketData`, `refreshMarketData`, `getMarketDataComponentConfig`, `saveMarketDataComponentConfig` on window.
  - Purpose: Central module that coordinates data fetching and component rendering.
  - _Leverage: `retail.js:546-573` for cached manifest data patterns; `loadData()`/`saveData()` from `js/utils.js`; `debugLog()` from `js/debug-log.js`; `safeGetElement()` from `js/utils.js`_
  - _Requirements: REQ-1, REQ-2, REQ-10_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Create `js/market-data.js` — the market data module orchestrator. (1) `initMarketData()`: async function called after spot data loads. Fetches `/v2/spot/{metal}/history-7d.json` for each metal (xag, xau, xpt, xpd) in parallel using `Promise.allSettled`. Cache each response in localStorage keys `v2SpotHistory_{metal}`. Save fetch timestamp to `v2SpotHistoryTs`. Read cached retail data from `loadData('v2ManifestCache')` and goldback data from `loadData('v2GoldbackData')` — if goldback not cached, fetch `/v2/goldback/latest.json` and cache it. Then call `renderMarketDataSection()`. (2) `renderMarketDataSection()`: reads `getMarketDataComponentConfig()`, gets container via `safeGetElement('marketDataContainer')`, clears it, then for each enabled component in config order, creates a wrapper `<div class="market-data-component" data-component="{id}">` and calls the appropriate render function: `enhancedSpot` → `renderEnhancedSpotCards(wrapper)`, `retailRanges` → `renderRetailPriceRanges(wrapper)`, `premiumHeatMap` → `renderPremiumHeatMap(wrapper)`, `goldbackCalc` → `renderGoldbackCalculator(wrapper)`, `bestPriceTicker` → `renderBestPriceTicker(wrapper)`. Use `typeof fn === 'function'` guards since chart functions come from market-charts.js. (3) `refreshMarketData()`: calls `renderMarketDataSection()` without re-fetching (uses cached data). (4) Stub render functions for the 3 CSS-only components (retailRanges, goldbackCalc, bestPriceTicker) — these will be filled in by tasks 7, 8, 9. (5) Export on window: `initMarketData`, `refreshMarketData`. (6) All fetch errors handled gracefully — log via `debugLog`, render from cache or show empty state. | Restrictions: Do NOT import modules — this is a vanilla JS file loaded via `<script defer>`. Use `safeGetElement` for all DOM access. Use `loadData`/`saveData` for localStorage. Sanitize any vendor/coin names with `sanitizeHtml` before DOM insertion. Do NOT use `var`. | Success: `initMarketData()` fetches v2 data, caches it, and renders the section. `refreshMarketData()` re-renders from cache. Components render in config order. Disabled components are skipped. Missing data shows neutral empty states. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 6. Enhanced Spot Cards + chart wrappers
  - File: `js/market-charts.js` (CREATE)
  - Create the chart wrapper module. `getChartThemeColors()` reads CSS custom properties from the DOM and returns a color config. `createSpotSparkline(container, data, metal)` creates a Lightweight Charts instance with an area series themed to the metal color. `destroySpotSparklines()` removes all instances. `renderEnhancedSpotCards(wrapper)` creates 4 spot cards with sparkline backgrounds. `createPremiumHeatMap(container, data)` creates an ApexCharts heatmap (stub — filled in task 8). `destroyPremiumHeatMap()` cleanup.
  - Purpose: Isolate chart library code from the orchestrator so CSS-only components work even if CDN fails.
  - _Leverage: TradingView Lightweight Charts docs: `createChart()` with `autoSize: true`, `addSeries(AreaSeries, {lineColor, topColor, bottomColor})`; `css/styles.css` theme variables; `safeGetElement()`; existing spot card DOM structure in `index.html:465-540` for reference_
  - _Requirements: REQ-3, REQ-8_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend chart developer | Task: Create `js/market-charts.js` — chart library wrappers for the market data module. Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design. (1) `getChartThemeColors()`: reads CSS custom properties from `document.documentElement` using `getComputedStyle`. Returns `{ bg: '--bg-card', text: '--text-primary', border: '--border', metals: { silver: '--silver', gold: '--gold', platinum: '--platinum', palladium: '--palladium' }, success: '--success', danger: '--danger' }` resolved to actual color values. (2) `renderEnhancedSpotCards(wrapper)`: creates a `.md-spot-cards-grid` container with 4 cards (silver, gold, platinum, palladium). Each card has: metal label, current spot price (from cached spot data on window or localStorage), 24h change with success/danger coloring, timestamp, and a Lightweight Charts area series as background. For the chart: check `typeof LightweightCharts !== 'undefined'` first. Create chart with `LightweightCharts.createChart(chartContainer, { autoSize: true, layout: { background: { type: 'solid', color: 'transparent' }, textColor: 'transparent' }, grid: { vertLines: { visible: false }, horzLines: { visible: false } }, timeScale: { visible: false }, rightPriceScale: { visible: false }, crosshair: { mode: 0 }, handleScroll: false, handleScale: false })`. Add area series with metal color at 30% opacity for fill. Set data from cached `v2SpotHistory_{metal}`. If no chart library or no history data, skip the chart (just show price). (3) `destroySpotSparklines()`: track chart instances in a module-level array, call `.remove()` on each. (4) `createPremiumHeatMap(container, data)`: stub function — will be filled by task 8. (5) `destroyPremiumHeatMap()`: stub. (6) Export on window: `renderEnhancedSpotCards`, `destroySpotSparklines`, `createPremiumHeatMap`, `destroyPremiumHeatMap`, `getChartThemeColors`. | Restrictions: Guard all chart library access with `typeof` checks — CDN may fail. Do NOT use Chart.js. Use `safeGetElement` for DOM access. No `var`. All colors from CSS custom properties via `getChartThemeColors()`. | Success: 4 spot cards render with metal labels, prices, change indicators, and area chart sparklines in the background. Cards are 2-col on mobile, 4-col at 960px+. Theme colors correct. Graceful degradation without chart library. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 7. Retail Price Ranges component
  - File: `js/market-data.js`
  - Implement `renderRetailPriceRanges(wrapper)`. CSS-only horizontal range bars showing low-median-high per coin, grouped by metal type. Read cached retail data. Calculate min/max/median across vendors for each coin. Render range bars with metal-colored fills and labeled values.
  - Purpose: Visualize the retail price spread across vendors for each coin.
  - _Leverage: Prototype `market-data-playground.html` range bar markup; cached retail data from `loadData`; `_manifestCoinMeta` for metal grouping; `sanitizeHtml()` for coin/vendor names_
  - _Requirements: REQ-4_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Implement `renderRetailPriceRanges(wrapper)` in `js/market-data.js` (replace the stub). Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design. (1) Read cached retail data — try `window._v2RetailData` or `loadData('v2ManifestCache')`. Also read `window._manifestCoinMeta` or `loadData('retailManifestCoinMeta')` for metal grouping. (2) For each coin, collect all vendor prices (excluding OOS), calculate min, max, median. (3) Group coins by metal type. (4) For each metal group, create a heading with a colored dot (metal color via CSS class). (5) For each coin in the group, create a `.md-range-bar-container` with: `.md-range-bar-label` (coin name + median price), `.md-range-bar` track with `.md-range-bar-fill` positioned proportionally from min to max, `.md-range-bar-median` marker at median position, `.md-range-bar-values` showing low and high amounts. (6) If a coin has only 1 vendor, show a single price point (no bar). (7) If no retail data available, show `.md-component-empty` with "No retail data available". (8) Sanitize all coin names with `sanitizeHtml()`. | Restrictions: CSS-only — no chart libraries. Use `safeGetElement` for DOM. `sanitizeHtml` on all text from API. No `var`. | Success: Range bars render for all coins with data, grouped by metal, showing low-median-high spread. Single-vendor coins show point value. Empty state shown when no data. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 8. Premium Heat Map component
  - File: `js/market-charts.js`
  - Implement `createPremiumHeatMap(container, data)` (replace the stub). ApexCharts heatmap with coins as rows (Y-axis), vendors as columns (X-axis). Cell value = premium % over spot. Color scale: green (low) → yellow (mid) → red (high) with vivid dark-mode colors. Carried-forward cells: dashed border treatment. OOS cells: reduced opacity. Hover tooltip: absolute price, premium $, vendor name.
  - Purpose: The primary vendor comparison tool — replaces the rejected bar chart approach.
  - _Leverage: ApexCharts heatmap docs via Context7; cached retail + spot data; `_manifestCoinMeta` for weights/metals; `_manifestVendorMeta` for vendor names; `getChartThemeColors()` from this file_
  - _Requirements: REQ-5, REQ-8_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend chart developer | Task: Implement `createPremiumHeatMap(container, data)` in `js/market-charts.js` (replace the stub). Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design. (1) Check `typeof ApexCharts !== 'undefined'` — if not available, show empty state and return. (2) Build series data: for each coin (Y-axis), for each vendor (X-axis), calculate premium %: `((vendorPrice - spotMeltValue) / spotMeltValue) * 100` where `spotMeltValue = coinWeight * spotPrice`. Read coin weights from `_manifestCoinMeta`, spot prices from cached data. (3) Configure ApexCharts: `{ chart: { type: 'heatmap', height: calculated based on row count, toolbar: { show: false } }, plotOptions: { heatmap: { shadeIntensity: 0.5, radius: 4, colorScale: { ranges: [{ from: 0, to: 5, name: 'Low', color: theme-green }, { from: 5.01, to: 15, name: 'Mid', color: theme-yellow }, { from: 15.01, to: 100, name: 'High', color: theme-red }] } } }, theme: { mode: isDarkOrSepia ? 'dark' : 'light' }, dataLabels: { enabled: true, formatter: val => val.toFixed(1) + '%' }, tooltip: { custom: function showing absolute price, premium $, vendor name }, xaxis: { categories: vendorNames } }`. (4) Use `getChartThemeColors()` for all colors. For dark mode, use saturated vivid greens/yellows/reds — NOT muted. Add new CSS custom properties if needed: `--md-heat-low` (bright green), `--md-heat-mid` (bright amber), `--md-heat-high` (bright red). (5) Handle carried-forward prices: add a CSS class to those cells after render for dashed border. (6) Handle OOS: set value to null or -1 with special color (grey, low opacity). (7) Container wrapped in `.md-heat-map-container` for mobile scroll. (8) Track the chart instance for cleanup in `destroyPremiumHeatMap()`. | Restrictions: No Chart.js. Guard ApexCharts access with typeof check. Use CSS custom properties for colors. No `var`. No hardcoded color hex in JS — read from DOM or define as CSS vars. | Success: Heat map renders with coins × vendors, premium % in each cell, vivid color scale, carried cells have dashed border, OOS cells are dimmed. Tooltip works on hover. Scrollable on mobile. Theme-correct in all 3 modes. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 9. Goldback Calculator component
  - File: `js/market-data.js`
  - Implement `renderGoldbackCalculator(wrapper)`. CSS-only denomination cards for G1, G5, G10, G25, G50. Show G1 rate, gold content per denomination, calculated values. State-level prices if available. Graceful 404 handling.
  - Purpose: Surface goldback pricing data in a user-friendly card layout.
  - _Leverage: Cached goldback data from `loadData('v2GoldbackData')`; prototype goldback section; `sanitizeHtml()`_
  - _Requirements: REQ-6_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Implement `renderGoldbackCalculator(wrapper)` in `js/market-data.js` (replace the stub). Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design. (1) Read goldback data from `loadData('v2GoldbackData')`. If null/undefined, show `.md-component-empty` with "Goldback data unavailable" in `var(--text-muted)` color (neutral, not error). (2) Display G1 rate prominently at the top (the base goldback price in USD). (3) Create a `.md-goldback-grid` with 5 denomination cards (G1, G5, G10, G25, G50). Each card shows: face value label, gold content in milligrams (G1=1/1000 ozt, G5=5/1000 ozt, etc.) and troy ounces, calculated melt value (gold content × gold spot price), and retail value if available. (4) If state-level prices exist in the data (keyed by state abbreviation), display them in a separate grouped section below the denomination cards. (5) Sanitize all text content with `sanitizeHtml()`. (6) Use CSS classes from task 4 (`.md-goldback-*`). | Restrictions: CSS-only — no chart libraries. Use `safeGetElement`, `loadData`, `sanitizeHtml`. No `var`. 404/missing data is EXPECTED (goldback scraper runs daily) — neutral messaging only, never error styling. | Success: Goldback calculator shows G1 rate + 5 denomination cards with calculated values. State prices shown if available. "Goldback data unavailable" shown gracefully when data missing. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 10. Best Price Ticker component
  - File: `js/market-data.js`
  - Implement `renderBestPriceTicker(wrapper)`. CSS-animated scrolling ticker showing cheapest in-stock vendor per coin. Filter out OOS coins. Static display if < 3 items.
  - Purpose: Quick-glance deal finder — shows where to get the best price for each coin.
  - _Leverage: Cached retail data; `_manifestCoinMeta` for coin names; `sanitizeHtml()`; CSS animation from task 4_
  - _Requirements: REQ-7_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend developer | Task: Implement `renderBestPriceTicker(wrapper)` in `js/market-data.js` (replace the stub). Read the approved prototype at `.spec-workflow/specs/STAK-504-market-data-module/artifacts/playground.html` for the visual design. (1) Read cached retail data. For each coin, find the vendor with the lowest in-stock price. Exclude coins where all vendors are OOS. (2) Build ticker items: each item shows coin name, vendor name, price, and premium % over spot (calculate as `((price - meltValue) / meltValue) * 100`). (3) If >= 3 items: render in a `.md-ticker` container with CSS scroll/rotate animation (CSS keyframes `@keyframes ticker-scroll` — horizontal translation loop). Duplicate items to create seamless loop. (4) If < 3 items: render statically in a flex row, no animation. (5) If no items at all: show `.md-component-empty` with "No retail prices available". (6) Sanitize all coin and vendor names with `sanitizeHtml()`. | Restrictions: CSS-only animation — no JS animation libraries. Use `safeGetElement`, `loadData`, `sanitizeHtml`. No `var`. | Success: Ticker shows cheapest vendor per coin, scrolling when 3+ items, static when fewer. OOS coins excluded. Empty state when no data. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

---

## Phase 4 — Integration (sequential — wires everything together)

- [ ] 11. Init integration and theme change handler
  - File: `js/init.js`
  - Add `initMarketData()` call after spot data resolves in the init sequence. Add theme change listener that calls `refreshMarketData()` (which destroys and re-creates charts with new theme colors). Verify the full data flow: page load → spot fetch → initMarketData() → v2 fetch → render components.
  - Purpose: Wire the market data module into the app lifecycle.
  - _Leverage: `js/init.js:555` where `applyLayoutOrder()` is called; `js/theme.js` for theme change events; `refreshMarketData()` from market-data.js_
  - _Requirements: REQ-1, REQ-8, REQ-10_
  - Recommended Agent: Claude
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend integration developer | Task: Wire `initMarketData()` into the app startup and add theme change handling. (1) In `js/init.js`, find where spot price loading completes and `applyLayoutOrder()` is called (~line 555). After `applyLayoutOrder()`, add: `if (typeof initMarketData === 'function') initMarketData().catch(e => { if (typeof debugLog === 'function') debugLog('[market-data] Init failed: ' + e.message, 'warn'); });`. The async call is intentionally fire-and-forget — market data is not blocking for app load. (2) Add a theme change handler. Find how theme changes are broadcast (check `js/theme.js` for custom events or callback patterns). Add a listener that calls: `if (typeof destroySpotSparklines === 'function') destroySpotSparklines(); if (typeof destroyPremiumHeatMap === 'function') destroyPremiumHeatMap(); if (typeof refreshMarketData === 'function') refreshMarketData();`. This destroys chart instances (which cache colors) then re-renders with fresh theme colors. (3) Verify the section is hidden/shown correctly by `applyLayoutOrder()` when toggled in Settings. | Restrictions: Do NOT modify the spot fetch logic. Do NOT block app init on market data. Use `typeof` guards for all market data functions. Do NOT modify theme.js — only add listeners. | Success: Page load triggers initMarketData after spot data. Market data section renders. Theme toggle destroys and re-creates charts with correct colors. Section toggle in Settings hides/shows the section. PREREQUISITE: Before writing any code, verify you are working inside a patch worktree (`git branch --show-current` must return patch/VERSION). If not, STOP and run /release patch first. Mark task as [-] in tasks.md before starting. BLOCKING: After implementation, you MUST call the log-implementation tool with full artifacts before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

---

## Standard Closing Tasks

- [ ] 12. Smoke test — verify no regressions
  - File: (no file changes — testing only)
  - Start local HTTP server, run `/bb-test` against the PR preview URL. Verify all existing tests pass and no console errors were introduced. Manually verify: market data section renders, all 5 components visible, theme switching works, Settings component picker works, mobile layout stacks correctly.
  - Purpose: Catch regressions before PR merge.
  - _Leverage: `/bb-test` skill; `npx serve /Volumes/DATA/GitHub/StakTrakr -p 8765`_
  - _Requirements: All_
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: QA engineer | Task: Run smoke tests for the market data module. (1) Run `/bb-test` against the PR preview URL. (2) Manually verify: market data section visible on page load, all 5 components render with data, toggle each component off/on in Settings and verify reflow, switch between all 3 themes and verify chart colors update, resize to mobile width and verify single-column stack, check browser console for errors. (3) Report pass/fail results. | Restrictions: Do not modify any source files. | Success: All existing tests pass. Market data module renders correctly. No console errors. All 3 themes work. Mobile layout correct. PREREQUISITE: This is a test-only task — no worktree changes needed. Mark task as [-] in tasks.md before starting. BLOCKING: After verification, you MUST call the log-implementation tool with the test results before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._

- [ ] 13. Update DocVault pages affected by this spec
  - File: (DocVault pages only — no production code changes)
  - Run `/vault-update` to detect and rewrite DocVault pages whose YAML frontmatter `sourceFiles` reference files changed by this spec. Expected pages: Frontend Overview, Architecture, API Consumption, DOM Patterns.
  - Purpose: Keep DocVault documentation current with the new market data module.
  - _Leverage: `/vault-update` skill; DocVault at `/Volumes/DATA/GitHub/DocVault/Projects/StakTrakr/`_
  - _Requirements: All_
  - _Prompt: Implement the task for spec STAK-504-market-data-module, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Technical writer | Task: Update DocVault pages affected by this spec. (1) Run the /vault-update skill — it detects which pages have `sourceFiles` frontmatter entries matching files changed in this spec (js/constants.js, js/settings.js, js/init.js, index.html, sw.js, css/styles.css). (2) Review each updated page for accuracy. (3) List all updated pages and what changed. | Restrictions: Only touch DocVault files. Do not modify production code. | Success: All affected DocVault pages updated and accurate. BLOCKING: After updates, you MUST call the log-implementation tool listing all updated pages before marking [x]. Do NOT mark [x] until the log-implementation tool call succeeds._
